PROJECT=$1
TAG=master

if [[ "x$PROJECT" == "x" ]]; then
  echo "No project given!"
  exit
fi

cp ~/.$PROJECT.json .
docker build -t isilanes/$PROJECT:$TAG -f Dockerfile.$PROJECT . 
rm -f .$PROJECT.json
