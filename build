PROJECT=$1
TAG=master

if [[ "x$PROJECT" == "x" ]]; then
  echo "No project given!"
  exit
fi

cp ~/.$PROJECT.json .
docker build -t isilanes/$PROJECT:$TAG -f conf/$PROJECT/Dockerfile . 
rm -f .$PROJECT.json
